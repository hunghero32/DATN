<div class="input-group {{$class}}">
    <input type="text"
           name="{{$name}}"
           id="{{$id}}"
           value="{{$value}}"
           placeholder="{{$placeholder}}"
           class="form-control"
           data-id="{{$dataId}}">
</div>
