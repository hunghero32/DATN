{{-- <footer class="content-footer footer bg-footer-theme">
  <div class="container-xxl d-flex flex-wrap justify-content-between py-2 flex-md-row flex-column">
    <div class="mb-2 mb-md-0">
      ©
      <script>
        document.write(new Date().getFullYear());
      </script>
      , made with ❤️ by {{ $system->site_name }}
    </div>
    <div>
      {{ $system->company_address ?? '' }}
      @if($system->company_email)
      <a href="mailto:{{ $system->company_email }}">{{ $system->company_email }}</a>
      @else
      @endif
      @if($system->company_phone)
      <a href="tel:{{ $system->company_phone }}">{{ $system->company_phone }}</a>
      @else
      @endif
    </div>
  </div>
</footer> --}}